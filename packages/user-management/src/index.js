// Punto de entrada del módulo user-management

module.exports = {
    init: function (config) {
        // Aquí se recibiría la configuración (modelos, adaptadores, etc.)
        // Ejemplo: this.userModel = config.userModel;
    },
    // Aquí se exportarán los servicios y controladores
};
